Mini independent project to learn Go, SQL, and HTTP endpoints for my Uber internship onboarding. The app parses incoming HTTP ad click traffic and dumps it into a non-blocking stream buffer. It spins up two concurrent goroutines in the background: one consumes the streaming click events safely in-memory, while the other runs on a timer to flush everything to a local SQLite database.
